Self-improvement system set up: 2 cron jobs (morning 9AM, evening 9PM), self-improvement skill at ~/.hermes/skills/self-improvement/. Jobs run RESEARCH→EVALUATE→TEST→INTEGRATE→REFLECT cycles against GitHub trending, ArXiv, HN, Reddit.
§
Self-improvement cycle 1 (2026-04-16): Searched GitHub trending, HN, Reddit. Found lean-ctx (609★, token compression 90+ patterns), mcp2cli (1965★, MCP→CLI), phantom (1267★, self-evolving agent with 3-tier memory). Binary tools fail on Termux/Android (TLS alignment). Integrated patterns as skills: command-compressor, three-tier-memory, evolution-gate. Key insight: extract architectural patterns from tools rather than installing binaries on Android.
§
Phone: Motorola, Android 13, aarch64, 3.8GB RAM. IP: 168.140.255.132. Only 3rd-party apps: Claude, Termux. Not rooted. Termux:API companion app install attempted (APK at /sdcard/Download/termux-api.apk) — user needs to manually install via file manager. ADB same-device pairing impossible (kernel limitation). Battery at 17-18%.
§
Android ADB same-device pairing: Termux ADB daemon CANNOT pair with the host Android's wireless debugging — they share the same kernel, "protocol fault" on all IPs. Need second device or Shizuku for input injection (tap, swipe, keyevent). Termux:API companion app must be installed from F-Droid (not Google Play). APK install via `pm install` fails due to SELinux — use `am start -a android.intent.action.VIEW -d "file:///path.apk" -t "application/vnd.android.package-archive"` to open system installer instead.
§
Running on Motorola Android 13 (aarch64) via Termux. Phone is minimal — only Claude and Termux as third-party apps. Termux:API companion app shows as "installed" by user but com.termux.api not visible in pm list — likely version mismatch between F-Droid Termux and Play Store Termux:API, or install didn't register properly. Same-device ADB pairing fails (kernel limitation). `input tap/swipe/keyevent` needs root or ADB shell. `settings` commands need INTERACT_ACROSS_USERS. `termux-battery-status` works without companion app. `am start` works for launching Settings screens and opening URLs.